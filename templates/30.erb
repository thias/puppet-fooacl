
# Sort directories
DIRSORTED="$(for DIR in "${KEY_MAP[@]}"; do echo $DIR; done | sort)"

# Apply the ACLs. Any existing ACLs will be replaced
for DIR in ${DIRSORTED}; do
  setkey $DIR
  setfacl ${ACLOPTS_GLOBAL} ${ACLOPTS[${KEY}]} ${DIR}
done
